# Claude Notes

Read [AGENTS.md](AGENTS.md) first. It is the canonical source for repository layout, build/test commands, ownership boundaries, privacy constraints, and focus-engine invariants.

Claude-specific reminder: regenerate with `xcodegen generate` after adding, removing, or renaming Swift files; then run the full `AnchoredTests` scheme before handoff.
